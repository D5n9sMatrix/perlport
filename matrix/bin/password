#!/bin/bash

password=$( dialog --stdout --inputbox 'Enter string to decode:' 0 0 )
echo "The decode: $password"

value=$( dialog --stdout --inputbox 'Decoded string value:' 0 0 )
echo "The value: $value"
